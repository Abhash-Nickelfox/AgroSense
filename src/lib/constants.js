// Update this to your real inbox — used by every "Let's Discuss" contact link.
export const CONTACT_EMAIL = "hello@agrosense.io";

export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}?subject=${encodeURIComponent(
  "Let's discuss AgroSense"
)}`;
